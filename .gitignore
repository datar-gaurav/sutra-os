# Credentials (NEVER commit)
**/google_credentials.json
**/credentials*.json
**/*.pem
**/*.key

# Python
__pycache__/
*.pyc
*.egg-info/
.venv/
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/

# Next.js
.next/
node_modules/
*.tsbuildinfo

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Environment
.env
.env.local
.env.*.local
backend/.env
backend/.ssh/

# Auto-generated from ALLOWED_AGENT_FILE_PATHS (see scripts/render-host-mounts.sh)
docker-compose.override.yml

# Backend Data & Cache
backend/.cache/
backend/.pki/
backend/data/
backend/.config/

# AI context
CLAUDE.md
.claude/

# Launch prep
.prep.md
docs/COMPARISONS.md

# Build
dist/
build/

# Root-level (not needed)
/node_modules/
/package.json
/package-lock.json

# Internal docs and audits (not for public repo)
docs/
security_audit.md

# Rendered launchd agents — machine-specific paths + secrets (tokens).
# Templates live in scripts/*.plist; start.sh/stop.sh load these by full path.
scripts/launchd/

# Host-run backend helpers — machine-specific paths + alternate DB/Redis ports.
backend/run-host.sh
docker-compose.hostports.yml
